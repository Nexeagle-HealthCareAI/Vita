import React from 'react';
import ReactDOM from 'react-dom/client';
import { ReceptionistDashboard } from './ReceptionistDashboard.js';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <ReceptionistDashboard />
  </React.StrictMode>,
);
